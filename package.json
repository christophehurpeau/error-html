{
  "name": "error-html",
  "version": "0.4.1",
  "description": "render error stack in html",
  "keywords": [],
  "homepage": "https://github.com/christophehurpeau/error-html",
  "bugs": {
    "url": "https://github.com/christophehurpeau/error-html/issues"
  },
  "license": "ISC",
  "author": "Christophe Hurpeau <christophe@hurpeau.com> (https://christophe.hurpeau.com)",
  "repository": "https://github.com/christophehurpeau/error-html.git",
  "files": [
    "src",
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index-node22.mjs",
  "types": "./dist/definitions/index.d.ts",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/definitions/index.d.ts",
      "node": {
        "import": "./dist/index-node22.mjs"
      }
    }
  },
  "scripts": {
    "build": "pnpm run clean:build && rollup --config rollup.config.mjs && pnpm run build:definitions",
    "build:definitions": "tsc -p tsconfig.json",
    "clean": "rm -Rf docs dist",
    "clean:build": "pob-esbuild-clean-out dist",
    "format": "oxfmt",
    "format:check": "oxfmt --check .",
    "generate:api": "typedoc --tsconfig tsconfig.doc.json",
    "lint": "pnpm run format && tsc && pnpm run lint:eslint",
    "lint:eslint": "eslint --quiet .",
    "prepare": "pob-root-postinstall",
    "preview": "node scripts/preview.ts",
    "test": "TZ=UTC node --test 'src/**/*.test.ts'",
    "test:coverage": "TZ=UTC node --test --experimental-test-coverage --test-coverage-include=\"src/**/*.ts\" 'src/**/*.test.ts'",
    "test:coverage:generate": "mkdir -p docs && TZ=UTC node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=docs/coverage.lcov --test-coverage-include=\"src/**/*.ts\" 'src/**/*.test.ts'",
    "test:e2e": "playwright test -c e2e/playwright.config.ts",
    "test:watch": "TZ=UTC node --test 'src/**/*.test.ts'",
    "tsc": "tsc",
    "watch": "pob-watch"
  },
  "dependencies": {
    "errorstacks": "^2.4.2",
    "escape-goat": "^4.0.0",
    "highlight.js": "^11.11.1"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@pob/rollup-esbuild": "10.0.0",
    "@pob/root": "27.2.1",
    "@types/node": "26.1.1",
    "@typescript/native": "npm:typescript@7.0.2",
    "eslint": "10.8.0",
    "typedoc": "0.28.20",
    "typescript": "npm:@typescript/typescript6@6.0.2"
  },
  "commitlint": {
    "extends": [
      "@pob/root/commitlint-config"
    ]
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "pob": {
    "bundler": "rollup-esbuild",
    "entries": [
      "index"
    ],
    "envs": [
      {
        "target": "node",
        "version": "22"
      }
    ],
    "typescript": true
  }
}
